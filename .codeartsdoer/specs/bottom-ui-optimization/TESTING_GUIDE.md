# 测试执行指南

## 🧪 测试准备

### 环境要求
- HarmonyOS SDK 已安装
- DevEco Studio 已配置
- 模拟器或真机已准备
- 项目已成功编译

### 测试工具
- DevEco Studio 内置调试器
- 日志查看工具
- 性能监控工具

---

## 📋 测试用例执行

### TC-001: 底部栏完整显示测试

**前置条件**: 应用已启动

**测试步骤**:
1. 启动应用，等待首页加载完成
2. 观察底部栏区域
3. 使用调试工具检查底部栏高度

**预期结果**:
- ✅ 底部栏完整显示，无裁剪
- ✅ 底部栏高度为72vp
- ✅ 背景色为深紫色(#3a3a5a)
- ✅ 底部栏与屏幕底部对齐

**验证方法**:
```typescript
// 在代码中添加日志
console.log('Bottom bar height:', HomeConstants.BOTTOM_BAR_HEIGHT);
```

**通过标准**: 所有预期结果均满足

---

### TC-002: 底部图标显示测试

**前置条件**: 应用已启动，底部栏显示正常

**测试步骤**:
1. 查看底部栏左侧功能项（音乐）
2. 查看底部栏右侧功能项（直播）
3. 检查图标清晰度
4. 检查图标尺寸

**预期结果**:
- ✅ 音乐功能显示音乐图标（ic_music_icon）
- ✅ 直播功能显示直播图标（ic_live）
- ✅ 图标清晰，无模糊或色块
- ✅ 图标尺寸为28x28vp
- ✅ 图标下方显示功能名称

**验证方法**:
- 视觉检查：图标应清晰可辨
- 尺寸检查：使用调试工具测量

**通过标准**: 图标清晰显示，尺寸正确

---

### TC-003: 底部功能项点击测试

**前置条件**: 应用已启动，底部栏显示正常

**测试步骤**:
1. 点击底部栏音乐功能项
2. 观察页面导航
3. 返回首页
4. 点击底部栏直播功能项
5. 观察页面导航

**预期结果**:
- ✅ 点击音乐项，导航到音乐列表页面
- ✅ 点击直播项，导航到直播页面
- ✅ 导航响应时间<50ms
- ✅ 页面切换流畅，无卡顿

**验证方法**:
```typescript
// 在onClick中添加日志
.onClick(() => {
  console.log('Tab clicked:', item.url);
  this.pageIndexInfos.pushPathByName(item.url, null);
})
```

**通过标准**: 导航功能正常，响应迅速

---

### TC-004: 卡片无按钮测试

**前置条件**: 应用已启动，首页显示正常

**测试步骤**:
1. 查看首页卡片区域
2. 检查音乐卡片内容
3. 检查直播卡片内容
4. 确认无按钮显示

**预期结果**:
- ✅ 卡片显示标题和描述
- ✅ 卡片显示背景图片
- ✅ 卡片内无"播放音乐"按钮
- ✅ 卡片内无"观看直播"按钮
- ✅ 卡片布局美观协调

**验证方法**:
- 视觉检查：确认无按钮元素
- 布局检查：卡片内容应简洁

**通过标准**: 卡片内无任何按钮元素

---

### TC-005: 卡片点击导航测试

**前置条件**: 应用已启动，卡片显示正常

**测试步骤**:
1. 点击音乐卡片左上角
2. 观察页面导航
3. 返回首页
4. 点击音乐卡片右下角
5. 观察页面导航
6. 对直播卡片重复上述步骤

**预期结果**:
- ✅ 点击卡片任意位置均可触发导航
- ✅ 点击音乐卡片，导航到音乐列表页面
- ✅ 点击直播卡片，导航到直播页面
- ✅ 点击响应时间<50ms
- ✅ 整个卡片区域都可点击

**验证方法**:
```typescript
// 在onClick中添加日志
.onClick(() => {
  console.log('Card clicked:', item.url);
  this.pageIndexInfos.pushPathByName(item.url, null);
})
```

**通过标准**: 整卡点击功能正常

---

### TC-006: 卡片点击反馈测试

**前置条件**: 应用已启动，卡片显示正常

**测试步骤**:
1. 按下音乐卡片（不松开）
2. 观察卡片透明度变化
3. 松开手指
4. 观察卡片透明度恢复
5. 对直播卡片重复上述步骤

**预期结果**:
- ✅ 按下时，卡片透明度变为0.8
- ✅ 松开后，卡片透明度恢复为1
- ✅ 状态切换流畅，无闪烁
- ✅ 视觉反馈明显
- ✅ 交互体验良好

**验证方法**:
- 视觉检查：透明度变化应明显
- 性能检查：状态切换应流畅

**通过标准**: 视觉反馈明显且流畅

---

### TC-007: UI一致性测试

**前置条件**: 应用已启动，所有UI元素显示正常

**测试步骤**:
1. 查看首页整体布局
2. 对比底部栏与顶部区域样式
3. 检查卡片样式
4. 检查文字样式
5. 检查图标样式

**预期结果**:
- ✅ 底部栏与顶部区域背景色一致(#3a3a5a)
- ✅ 文字颜色为白色
- ✅ 文字大小符合设计规范
- ✅ 图标大小符合设计规范
- ✅ 整体风格统一协调
- ✅ 无视觉异常

**验证方法**:
- 视觉检查：整体风格应统一
- 样式检查：颜色、大小应一致

**通过标准**: UI风格统一协调

---

## 📊 测试结果记录

### 测试结果表

| 测试用例 | 执行时间 | 结果 | 备注 |
|---------|---------|------|------|
| TC-001 | - | ⏳ 待测试 | - |
| TC-002 | - | ⏳ 待测试 | - |
| TC-003 | - | ⏳ 待测试 | - |
| TC-004 | - | ⏳ 待测试 | - |
| TC-005 | - | ⏳ 待测试 | - |
| TC-006 | - | ⏳ 待测试 | - |
| TC-007 | - | ⏳ 待测试 | - |

### 结果统计
- **通过**: 0个
- **失败**: 0个
- **待测试**: 7个
- **通过率**: 0%

---

## 🔍 问题记录

### 问题模板
```
问题ID: BUG-XXX
测试用例: TC-XXX
问题描述: [详细描述]
重现步骤: [步骤列表]
预期结果: [预期行为]
实际结果: [实际行为]
严重程度: [高/中/低]
```

---

## ✅ 测试完成标准

### 全部通过标准
- [ ] 所有7个测试用例执行完成
- [ ] 所有测试用例结果为通过
- [ ] 无严重问题
- [ ] 无功能缺陷
- [ ] UI显示正常
- [ ] 交互流畅

### 可接受标准
- [ ] 至少6个测试用例通过
- [ ] 无严重问题
- [ ] 无功能缺陷
- [ ] 轻微问题可接受

---

## 📝 测试报告

### 测试总结
- **测试时间**: [填写日期]
- **测试人员**: [填写姓名]
- **测试环境**: [填写环境]
- **测试结果**: [通过/失败]

### 问题汇总
- **严重问题**: 0个
- **一般问题**: 0个
- **轻微问题**: 0个

### 建议
- [填写测试建议]

---

## 🚀 测试执行流程

1. **准备阶段** (5分钟)
   - 确认环境准备就绪
   - 启动应用
   - 准备测试工具

2. **执行阶段** (15分钟)
   - 按顺序执行测试用例
   - 记录测试结果
   - 记录问题（如有）

3. **总结阶段** (5分钟)
   - 统计测试结果
   - 编写测试报告
   - 提交测试报告

**总耗时**: 约25分钟

---

**测试指南版本**: v1.0
**更新时间**: 2026-05-26
